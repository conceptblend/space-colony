const presets = {
  "zero": {
    lifespan: 48,
    maxDist: 96,
    minDist: 24,
    branchLength: 12,
    attractors: 25000,
    angle: 120,
    canvasSize: 540 * 2
  }
};

const CONFIG = presets.zero;